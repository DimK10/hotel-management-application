import React from 'react';
import PropTypes from 'prop-types';

const NotFound = props => {
  return (
    <div>
        404 not found
    </div>
  );
};

NotFound.propTypes = {

};

export default NotFound;
